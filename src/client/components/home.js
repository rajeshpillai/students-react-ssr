import React from 'react';

const Home = () => {
    return (
        <div>
            <h2>Home is where Heart is!</h2>
            <button onClick={() => console.log("hello!")}>Click me!</button>
        </div>
    );
};
export default Home;